name: Build Animaca Agent v0.7

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  upgrade:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Migração v0.7 já aplicada. Workflow preservado apenas para histórico até a finalização da branch."
